{
  "buildCommand": "mkdir -p backend/app/_corpus && cp eval/output/samples.json backend/app/_corpus/samples.json && cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" }
  ],
  "functions": {
    "api/index.py": {
      "maxDuration": 300,
      "memory": 2048
    }
  }
}
