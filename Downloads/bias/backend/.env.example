# BiasScan does NOT read API keys from the environment.
# Keys are entered in the frontend, sent per request, and never stored.
#
# The only env vars are tuning knobs (all optional):

# BIASSCAN_CONFIDENCE_FLOOR=0.5
# BIASSCAN_MAX_TOKENS=4096
# BIASSCAN_PROVIDER_TIMEOUT=180
